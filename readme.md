# ChebChoiz (Menu Randomizer Project)

**ChebChoiz** คือเว็บแอปพลิเคชันสำหรับช่วยตัดสินใจเลือกเมนูอาหาร (Food Randomizer) ออกแบบมาเพื่อแก้ปัญหาโลกแตก "วันนี้กินอะไรดี?" โดยผู้ใช้สามารถสุ่มอาหารตามหมวดหมู่ ราคา หรือสถานการณ์ได้ รวมถึงสามารถบันทึกประวัติการสุ่มและจัดการรายการเมนูของตัวเองได้

## 🌟 ฟีเจอร์หลัก (Features)

### 1. 🎲 ระบบสุ่มอาหาร (Menu Randomizer)
- สุ่มเมนูอาหารจากฐานข้อมูล
- **Filter (ตัวกรอง):** สามารถกรองเมนูที่จะสุ่มได้ 3 รูปแบบ:
  - **Price:** กรองตามช่วงราคา (เช่น 50-100 บาท, 450 บาทขึ้นไป)
  - **Category:** กรองตามประเภทอาหาร (เช่น เส้น, ข้าว, ซุป, บุฟเฟต์, อาหารไทย, อาหารอิตาเลียน ฯลฯ)
  - **Situation:** กรองตามโอกาส (เช่น อาหารเช้า, อาหารจานเดียว, กินเลี้ยงฉลอง)
- แสดงผลการสุ่มพร้อมรูปภาพและรายละเอียด

### 2. 📜 ประวัติการใช้งาน (History)
- บันทึกรายการอาหารที่ผู้ใช้เคยกดสุ่มหรือเลือกไว้
- แสดงรายละเอียด: ชื่อเมนู, ราคา, หมวดหมู่, และวันที่สุ่ม
- **Sorting:** เรียงลำดับประวัติได้ (ล่าสุด, เก่าสุด, ราคาต่ำ-สูง, ราคาสูง-ต่ำ)
- เชื่อมโยงกับบัญชีผู้ใช้ (User ID)

### 3. 🛠️ การจัดการเมนู (Menu Management)
- **Custom Menu:** ผู้ใช้สามารถเพิ่มเมนูอาหารของตัวเองได้ (Add Menu)
- **Edit/Delete:** แก้ไขหรือลบเมนูที่สร้างขึ้นเองได้
- **Hide System Menu:** สามารถซ่อนเมนูมาตรฐานของระบบที่ไม่ต้องการให้แสดงในการสุ่มได้
- **Reset:** ปุ่มรีเซ็ตเพื่อล้างการตั้งค่าเมนูทั้งหมดกลับเป็นค่าเริ่มต้น

### 4. 👤 ระบบสมาชิก (User System)
- สมัครสมาชิก (Register) และเข้าสู่ระบบ (Login)
- ข้อมูลเมนูและประวัติจะถูกแยกตามบัญชีผู้ใช้ (User Specific Data)
- รองรับการเปลี่ยนรหัสผ่าน (Reset Password)

---

## 💻 เทคโนโลยีที่ใช้ (Tech Stack)

### Frontend
- **HTML5 / CSS3:** โครงสร้างเว็บและการตกแต่ง (ใช้ Flexbox/Grid และฟอนต์ 'Kanit')
- **JavaScript (Vanilla):** การทำงานฝั่งหน้าบ้าน, การเชื่อมต่อ API (Fetch), และ DOM Manipulation

### Backend
- **Node.js:** Runtime Environment
- **Express.js:** Web Framework สำหรับสร้าง API
- **MySQL2:** Library สำหรับเชื่อมต่อฐานข้อมูล MySQL

### Database
- **MySQL:** ระบบฐานข้อมูล Relational Database เก็บข้อมูล User, Menu, Price, Category, Situation และ History

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
ChebChoiz/
├── img/                        # โฟลเดอร์เก็บรูปภาพอาหารและไอคอน
├── backend/
│   └── ChebChoiz/
│       └── my-mysql-api/       # โฟลเดอร์ Backend API
│           ├── index.js        # ไฟล์หลักของ Server (Express + Database Config)
│           ├── package.json    # รายชื่อ Dependencies (express, mysql2, cors, dotenv)
│           └── .env            # (ต้องสร้างเอง) เก็บค่า Config ฐานข้อมูล
├── ChebChoiz_main.html         # หน้าหลัก (Randomizer)
├── ChebChoiz_main.js           # สคริปต์หน้าหลัก
├── ChebChoiz_main.css          # สไตล์หน้าหลัก
├── ChebChoiz_history.html      # หน้าประวัติ
├── ChebChoiz_history.js        # สคริปต์หน้าประวัติ
├── ChebChoiz_listmenu.html     # หน้าจัดการเมนู (Add/Edit/Delete)
├── ChebChoiz_listmenu.js       # สคริปต์หน้าจัดการเมนู
├── ChebChoiz_login.html        # หน้า Login
├── ChebChoiz_signup.html       # หน้าสมัครสมาชิก
└── ... (ไฟล์ CSS/JS อื่นๆ)

Method,     Endpoint,               รายละเอียด
Auth,,
POST,       /api/register,          สมัครสมาชิกใหม่
POST,       /api/login,             เข้าสู่ระบบ
POST,       /api/reset-password,    เปลี่ยนรหัสผ่าน

Menu,,
GET,        /api/menu,              ดึงข้อมูลเมนู (รองรับ Query ?userId=... เพื่อดึงเมนูของตัวเอง)
POST,       /api/menu/add,          เพิ่มเมนูใหม่ (Custom Menu)
POST,       /api/menu/edit,         แก้ไขเมนู
POST,       /api/menu/delete,       ลบเมนู (หรือซ่อนเมนูระบบ)
POST,       /api/menu/reset,        รีเซ็ตเมนูทั้งหมดของ User

History,,
GET,        /api/history/:userId,   ดึงประวัติการสุ่มของผู้ใช้
POST,       /api/history,           บันทึกผลการสุ่มลงประวัติ